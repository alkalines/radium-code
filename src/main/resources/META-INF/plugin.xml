<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>net.alkalines.radiumcode.radium-code</id>

    <!-- Public plugin name should be written in Title Case.
     Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-name -->
    <name>Radium Code</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/alkalines/radium-code">Alkalines</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/best-practices-for-listing.html#plugin-description -->
    <description><![CDATA[
        Radium Code is an IDE-native AI agent for JetBrains IDEs that keeps the conversation close to project context, model streaming, and future tool use inside the tool window.
        ]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.compose</depends>

    <resource-bundle>messages.AgentMessageBundle</resource-bundle>

    <!-- Extensions defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <iconMapper mappingFile="RadiumCodeIconMappings.json"/>
        <notificationGroup id="Radium Code" displayType="BALLOON" isLogByDefault="true"/>
        <applicationService
                serviceImplementation="net.alkalines.radiumcode.agent.config.AgentModelConfigStore"/>
        <toolWindow id="AgentToolWindow"
                    factoryClass="net.alkalines.radiumcode.AgentToolWindowFactory"
                    anchor="left"
                    icon="/META-INF/toolWindowIcon.svg"/>
    </extensions>

</idea-plugin>
